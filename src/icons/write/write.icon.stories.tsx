import type { Meta, StoryObj } from "@storybook/react";
import { WriteIcon } from "./write.icon";


const meta: Meta<typeof WriteIcon> = {
  title: "ICONS/WriteIcon",
  component: WriteIcon,
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj<typeof WriteIcon>;

export const Primary: Story = {
  args: {
    
  },
};

